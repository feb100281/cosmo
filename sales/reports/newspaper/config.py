# sales/reports/newspaper/config.py
"""
Общие константы и настройки для отчёта COSMORELAX Daily Brief.

Это единственное место, где живут "магические числа" и визуальные
константы отчёта — цвета, пороги для аналитических сигналов, подписи.
Модули data/analytics/charts/render читают эти значения отсюда, а не
хардкодят их у себя.
"""

from __future__ import annotations

# ---------------------------------------------------------------------------
# Бренд / шапка отчёта
# ---------------------------------------------------------------------------

COMPANY_LEGAL_NAME = 'ООО "КОСМО"'
COMPANY_BRAND_NAME = "COSMORELAX"
COMPANY_WEBSITE = "www.cosmorelax.ru"
REPORT_TITLE = "COSMORELAX Daily Brief"
REPORT_SUBTITLE = "Управленческая сводка: кэш и остатки"

# Имя файла на выходе: COSMORELAX_Daily_Brief_2026-08-18.pdf
PDF_FILENAME_TEMPLATE = "COSMORELAX_Daily_Brief_{date}.pdf"

# ---------------------------------------------------------------------------
# Цветовая палитра (editorial / financial newspaper)
#
# Небольшое число акцентных цветов: нейтральный графит для текста и сетки,
# один фирменный акцент, и два строго зарезервированных цвета для
# положительных/отрицательных отклонений. Цвет здесь несёт смысл, а не
# декорацию.
# ---------------------------------------------------------------------------

COLOR_INK = "#1c2321"          # основной текст (тёплый графитово-зелёный)
COLOR_INK_SOFT = "#5b6b63"     # вторичный текст / подписи
COLOR_RULE = "#d7ddd3"         # тонкие линии-разделители
COLOR_PAPER = "#f7f8f3"        # фон полосы (тёплый почти-белый с зелёным оттенком)
COLOR_ACCENT = "#c1663a"       # фирменный акцент (терракотовый — тёплый, не бордовый)
COLOR_ACCENT_SOFT = "#e3b48f"
COLOR_BRAND_DEEP = "#1e3d32"   # глубокий зелёный — плашки шапки/заголовков таблиц

COLOR_POSITIVE = "#1e6b4f"     # положительное отклонение / выполнение плана
COLOR_POSITIVE_BG = "#e2efe6"
COLOR_NEGATIVE = "#b8492f"     # отрицательное отклонение / отставание (тёплый терракотовый-красный)
COLOR_NEGATIVE_BG = "#f6e6e0"
COLOR_NEUTRAL = "#8c9188"

# Палитра для многосерийных диаграмм (доли магазинов и т.п.) — тёплая,
# зелёно-терракотовая гамма под бренд COSMORELAX, без "кислотных" цветов.
CHART_PALETTE = [
    "#1e6b4f",
    "#c1663a",
    "#3f8073",
    "#d9a441",
    "#6f8f7a",
    "#8a5a3c",
    "#4f6b60",
    "#b6875e",
]

FONT_SERIF = "Georgia, 'Times New Roman', serif"
FONT_SANS = "'Helvetica Neue', Arial, sans-serif"

# ---------------------------------------------------------------------------
# Пороги для автоматической аналитики (детерминированные правила, без LLM)
# ---------------------------------------------------------------------------

# Выполнение плана по кэшу ниже этого % от "плана на сегодня" (linear pace)
# считается заметным отставанием.
CASH_PACE_LAG_ALERT_PCT = 10          # отставание от линейного темпа, п.п.
CASH_STORE_LAG_ALERT_PCT = 15         # магазин отстаёт от среднего темпа сети, п.п.
CASH_STRONG_DEVIATION_PCT = 20        # "сильное" отклонение прогноза от плана, %

# Остатки: концентрация и дисбаланс (по количеству — денежной оценки
# остатков в отчёте нет, см. data/stocks.py)
STOCK_TOP_CATEGORY_CONCENTRATION_PCT = 50   # доля топ-3 категорий, выше которой — сигнал
STOCK_STORE_IMBALANCE_PP = 15               # разница доли остатков и доли кэша магазина, п.п.

# ---------------------------------------------------------------------------
# Верстка PDF
# ---------------------------------------------------------------------------

# Альбомная ориентация — таблица по магазинам (10 колонок) и недельный
# календарь (8 колонок) не помещались по ширине в портретной A4 без потери
# читаемости. Реальный источник истины — @page в report.css; здесь то же
# самое значение хранится для документации/повторного использования.
PAGE_SIZE = "A4 landscape"
PAGE_MARGIN = "10mm 12mm 14mm 12mm"

MAX_INSIGHTS_ON_COVER = 4
